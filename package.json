{
  "name": "my-api-mocha-test",
  "version": "1.0.0",
  "description": "net Core api Tests",
  "author": "Dom",
  "private": true,
  "scripts": {
    "dev": "webpack-dev-server --inline --progress --config build/webpack.dev.conf.js",
    "start": "npm run dev",
    "test": "npm run server",
    "server": "mocha --timeout 10000 tests.specs.js",
    "build": "node build/build.js"
  },
  "dependencies": {
    "mocha": "^5.2.0",
    "supertest": "^3.1.0"
  }
}
